// Standalone HTML player runtime JS and styling CSS
export const STANDALONE_PLAYER_JS = '/* 导出单文件 HTML 的播放器运行时(纯 JS,依赖同页的 PosterEngine 与 window.POSTER_CONFIG) */\n(function () {\n\'use strict\';\nvar E = window.PosterEngine, cfg = window.POSTER_CONFIG;\nvar $ = function (id) { return document.getElementById(id); };\nvar cv = $(\'cv\'); cv.width = E.W; cv.height = E.H;\nvar R = E.createRenderer(cv, cfg), T = R.model.T;\nvar Tt = 0, playing = true, looping = true, slow = false, last = performance.now();\nvar qs = new URLSearchParams(location.search);\n\nvar rail = $(\'phases\');\nR.model.scenes.forEach(function (s, i) {\n  var d = document.createElement(\'div\'); d.className = \'ph\';\n  d.innerHTML = \'<span class="no">\' + String(i + 1).padStart(2, \'0\') + \'</span><span class="nm">\' + s.label + \' · \' + s.name + \'</span><span class="tm">\' + s.quote + \'<br>\' + s.tStart.toFixed(1) + \'s 起</span>\';\n  d.addEventListener(\'click\', function () { Tt = s.tStart; playing = true; sync(); });\n  rail.appendChild(d);\n});\nvar phEls = Array.prototype.slice.call(rail.children);\nfunction fmt(s) { var m = Math.floor(s / 60), ss = s % 60; return m + \':\' + (ss < 10 ? \'0\' : \'\') + ss.toFixed(1); }\nfunction sync() { $(\'bPause\').textContent = playing ? \'⏸ 暂停\' : \'▶ 播放\'; }\nfunction frame(now) {\n  var dt = Math.min(.1, (now - last) / 1000); last = now;\n  if (playing) { Tt += dt * (slow ? .5 : 1); if (Tt > T) { if (looping) Tt %= T; else { Tt = T; playing = false; sync(); } } }\n  R.draw(Math.min(Tt, T - .001));\n  $(\'clk\').textContent = fmt(Math.min(Tt, T)) + \' / \' + fmt(T);\n  $(\'pfill\').style.width = (Math.min(Tt, T) / T * 100) + \'%\';\n  var si = R.model.sceneIndexAt(Math.min(Tt, T));\n  phEls.forEach(function (el, i) { el.classList.toggle(\'on\', si === i); });\n  requestAnimationFrame(frame);\n}\nrequestAnimationFrame(frame);\n$(\'bReplay\').addEventListener(\'click\', function () { Tt = 0; playing = true; sync(); });\n$(\'bPause\').addEventListener(\'click\', function () { playing = !playing; sync(); });\n$(\'bLoop\').addEventListener(\'click\', function (e) { looping = !looping; e.currentTarget.setAttribute(\'aria-pressed\', looping); });\n$(\'bSlow\').addEventListener(\'click\', function (e) { slow = !slow; e.currentTarget.setAttribute(\'aria-pressed\', slow); });\ncv.addEventListener(\'click\', function () { playing = !playing; sync(); });\n$(\'pbar\').addEventListener(\'click\', function (e) { var r = e.currentTarget.getBoundingClientRect(); Tt = T * (e.clientX - r.left) / r.width; });\nwindow.addEventListener(\'keydown\', function (e) {\n  var tag = (e.target.tagName || \'\').toLowerCase(); if (tag === \'input\' || tag === \'textarea\') return;\n  var sc = R.model.scenes, si = R.model.sceneIndexAt(Tt);\n  if (e.code === \'Space\') { e.preventDefault(); playing = !playing; sync(); }\n  else if (e.key === \'ArrowRight\') { var n = sc[Math.min(sc.length - 1, si + 1)]; if (n) { Tt = n.tStart; playing = true; sync(); } }\n  else if (e.key === \'ArrowLeft\') { var p = sc[Math.max(0, si - 1)]; if (p) { Tt = p.tStart; playing = true; sync(); } }\n});\n\n/* 录制 WebM(MediaRecorder,实时录一整轮) */\nvar recording = false;\n$(\'bRec\').addEventListener(\'click\', function () {\n  if (recording || typeof MediaRecorder === \'undefined\') { $(\'exp\').textContent = recording ? \'录制中…\' : \'当前浏览器不支持 MediaRecorder\'; return; }\n  var stream = cv.captureStream(30), chunks = [];\n  var mime = [\'video/webm;codecs=vp9\', \'video/webm;codecs=vp8\', \'video/webm\'].filter(function (m) { return MediaRecorder.isTypeSupported(m); })[0] || \'\';\n  var rec; try { rec = new MediaRecorder(stream, mime ? { mimeType: mime, videoBitsPerSecond: 8000000 } : undefined); } catch (err) { $(\'exp\').textContent = \'无法录制:\' + err.message; return; }\n  rec.ondataavailable = function (e) { if (e.data.size) chunks.push(e.data); };\n  rec.onstop = function () {\n    var b = new Blob(chunks, { type: \'video/webm\' }); var a = document.createElement(\'a\'); a.href = URL.createObjectURL(b); a.download = (cfg.title || \'poster\').replace(/[\\\\s·]+/g, \'-\') + \'.webm\'; a.click();\n    setTimeout(function () { URL.revokeObjectURL(a.href); }, 2000); recording = false; looping = wasLoop; $(\'exp\').textContent = \'✓ 已保存 WebM(\' + (b.size / 1048576).toFixed(1) + \' MB)\'; $(\'bRec\').textContent = \'● 录制 WebM\';\n  };\n  var wasLoop = looping; looping = false; Tt = 0; playing = true; slow = false; $(\'bSlow\').setAttribute(\'aria-pressed\', \'false\'); sync();\n  recording = true; rec.start(200); $(\'bRec\').textContent = \'■ 录制中…\'; $(\'exp\').textContent = \'录制中,将实时走完一整轮(\' + fmt(T) + \')…\';\n  var tick = setInterval(function () { if (Tt >= T - .02 || !recording) { clearInterval(tick); if (rec.state !== \'inactive\') rec.stop(); } }, 100);\n});\n$(\'bSrt\').addEventListener(\'click\', function () {\n  var b = new Blob([R.model.srt()], { type: \'text/plain;charset=utf-8\' }); var a = document.createElement(\'a\'); a.href = URL.createObjectURL(b); a.download = \'subtitles.srt\'; a.click(); setTimeout(function () { URL.revokeObjectURL(a.href); }, 1500);\n  $(\'exp\').textContent = \'✓ 已导出 SRT 字幕(\' + R.model.scenes.length + \' 条)\';\n});\nif (qs.get(\'t\') != null) { Tt = parseFloat(qs.get(\'t\')) || 0; playing = false; sync(); }\nif (qs.get(\'clean\') === \'1\') document.body.classList.add(\'clean\');\n})();\n';

export const STANDALONE_PLAYER_CSS = ':root{--ink:#17120e;--verm:#c8472b;--gold:#c99a3f;--txt:#e8ddc8;--dim:rgba(232,221,200,.55)}\n*{margin:0;padding:0;box-sizing:border-box}\nbody{font-family:"Noto Sans SC","PingFang SC","Microsoft YaHei",sans-serif;color:var(--txt);min-height:100vh;overflow-x:hidden;\n background:radial-gradient(1000px 700px at 12% -8%,rgba(200,71,43,.10),transparent 55%),radial-gradient(900px 700px at 95% 100%,rgba(90,110,150,.12),transparent 55%),repeating-linear-gradient(135deg,rgba(255,240,210,.012) 0 1px,transparent 1px 8px),var(--ink)}\n.glyph{position:fixed;right:-24px;top:50%;transform:translateY(-50%);writing-mode:vertical-rl;font-family:"Noto Serif SC","Songti SC",serif;font-weight:900;font-size:clamp(140px,22vw,300px);color:rgba(232,221,200,.035);pointer-events:none;user-select:none;z-index:0}\n.wrap{max-width:1200px;margin:0 auto;padding:0 22px;position:relative;z-index:1}\nheader{display:flex;align-items:center;gap:14px;padding:26px 0 8px;flex-wrap:wrap}\n.hseal{width:46px;height:46px;background:var(--verm);color:#fff;display:grid;place-items:center;border-radius:8px;font-family:"Ma Shan Zheng","Kaiti SC",cursive;font-size:28px;transform:rotate(-4deg);box-shadow:3px 4px 0 rgba(0,0,0,.35)}\nheader h1{font-family:"Noto Serif SC","Songti SC",serif;font-weight:900;font-size:clamp(22px,3.4vw,32px);letter-spacing:.06em}\nheader h1 em{font-style:normal;color:var(--verm)}\n.hmeta{margin-left:auto;display:flex;gap:8px;flex-wrap:wrap}\n.hmeta span{font-size:11px;letter-spacing:.12em;color:var(--dim);border:1px solid rgba(232,221,200,.18);padding:4px 10px;border-radius:2px}\nmain{display:flex;gap:34px;align-items:flex-start;justify-content:center;padding:22px 0 10px;flex-wrap:wrap}\n.stage{display:flex;flex-direction:column;align-items:center;gap:14px}\ncanvas#cv{height:min(78vh,820px);aspect-ratio:9/16;width:auto;cursor:pointer;border:1px solid rgba(201,154,63,.35);border-radius:4px;box-shadow:0 30px 80px rgba(0,0,0,.55),0 0 0 6px rgba(255,245,225,.03),0 0 120px rgba(200,71,43,.07)}\n@media(max-width:600px){canvas#cv{height:auto;width:92vw}}\n.pbar{width:min(78vh*(9/16),820px*(9/16));max-width:92vw;height:4px;background:rgba(232,221,200,.12);overflow:hidden;border-radius:2px;cursor:pointer}\n@media(max-width:600px){.pbar{width:92vw}}\n#pfill{display:block;height:100%;width:0%;background:linear-gradient(90deg,var(--verm),var(--gold))}\n.cap{font-size:12px;color:var(--dim);letter-spacing:.08em;text-align:center}\n.rail{display:flex;flex-direction:column;gap:10px;min-width:260px;max-width:320px;padding-top:8px}\n.rail .rt{font-size:11px;letter-spacing:.35em;color:var(--dim);margin-bottom:2px}\n.ph{display:flex;align-items:center;gap:12px;padding:10px 14px;border:1px solid rgba(232,221,200,.14);border-left:3px solid transparent;background:rgba(255,245,225,.02);cursor:pointer;transition:.22s;border-radius:2px;margin-bottom:6px}\n.ph:hover{transform:translateX(4px);border-color:rgba(201,154,63,.4)}\n.ph.on{border-left-color:var(--verm);background:rgba(200,71,43,.12);transform:translateX(6px)}\n.ph .no{font-family:"Noto Serif SC",serif;font-weight:900;color:var(--gold);font-size:15px;width:22px}\n.ph .nm{font-size:13.5px;font-weight:600;letter-spacing:.08em;white-space:nowrap}\n.ph .tm{margin-left:auto;font-size:10.5px;color:var(--dim);text-align:right;line-height:1.5}\n.ph.on .nm{color:#fff}\n.ctrl{display:flex;gap:8px;flex-wrap:wrap;margin-top:6px}\n.btn{font:inherit;font-size:13px;font-weight:700;letter-spacing:.1em;padding:9px 16px;cursor:pointer;border-radius:2px;border:1px solid rgba(232,221,200,.3);background:transparent;color:var(--txt);transition:.18s}\n.btn:hover{transform:translateY(-2px);border-color:var(--gold);box-shadow:0 8px 18px rgba(0,0,0,.4)}\n.btn.pri{background:var(--verm);border-color:var(--verm);color:#fff}\n.btn.tgl[aria-pressed="true"]{background:var(--gold);border-color:var(--gold);color:#1c150c}\n.clock{font-family:ui-monospace,monospace;font-size:12px;color:var(--dim);margin-top:10px}\n.clock b{color:var(--gold)}\n.exp{font:500 12px ui-monospace,monospace;color:var(--dim);margin-top:8px;min-height:16px;line-height:1.5}\nfooter{padding:20px 0 34px;font-size:11.5px;color:rgba(232,221,200,.4);line-height:1.8}\nbody.clean header,body.clean .rail,body.clean .pbar,body.clean .cap,body.clean footer,body.clean .glyph{display:none}\nbody.clean{display:grid;place-items:center;min-height:100vh}\nbody.clean main{padding:0}\nbody.clean canvas#cv{height:min(96vh,160vw);box-shadow:none}\n';
